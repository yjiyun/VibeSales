import * as fs from 'fs'; import * as path from 'path'; import { Pool } from 'pg';
const admin=process.env.DATABASE_ADMIN_URL?.trim(),appPassword=process.env.CHATFLOWS_APP_DB_PASSWORD?.trim(),runtimePassword=process.env.AGENT_RUNTIME_DB_PASSWORD?.trim();if(!admin||!appPassword||!runtimePassword)throw new Error('DATABASE_ADMIN_URL/CHATFLOWS_APP_DB_PASSWORD/AGENT_RUNTIME_DB_PASSWORD required');
async function main(){const pool=new Pool({connectionString:admin,max:1,ssl:process.env.DATABASE_SSL==='1'?{rejectUnauthorized:true}:undefined});try{await pool.query(fs.readFileSync(path.resolve(__dirname,'../sql/001_agentteams.sql'),'utf8'));for(const [login,password,parent] of [['chatflows_app_login',appPassword,'chatflows_app'],['agent_runtime_login',runtimePassword,'agent_runtime']] as const){const exists=await pool.query<{exists:boolean}>('select exists(select 1 from pg_roles where rolname=$1)',[login]);const statement=exists.rows[0].exists?'alter role %I login inherit nosuperuser nocreatedb nocreaterole noreplication password %L':'create role %I login inherit nosuperuser nocreatedb nocreaterole noreplication password %L';const formatted=await pool.query<{sql:string}>('select format($1::text,$2::text,$3::text) as sql',[statement,login,password]);await pool.query(formatted.rows[0].sql);await pool.query('grant '+parent+' to '+login);}process.stdout.write('[PASS] initialized AgentTeams schema and least-privilege LOGIN roles\n');}finally{await pool.end();}}
main().then(()=>process.exit(0)).catch(error=>{process.stderr.write((error instanceof Error?error.message:String(error))+'\n');process.exit(1);});
